meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"

build:
  requirementsPath: requirements.txt
  useCache: true

run:
  command: gunicorn app.wsgi:application --bind 0.0.0.0:80 --workers 2
  persistenceMount: /data
  containerPort: "80"
  servicePort: "80"
